import { RoleUsersEnum } from '../enums/enums';
import { RoleUsers } from '../type/sharedTypes';

/**
 * Array com todos os valores do enum — útil para validações.
 */
export const ALL_ROLES: RoleUsers[] = Object.values(RoleUsersEnum);
